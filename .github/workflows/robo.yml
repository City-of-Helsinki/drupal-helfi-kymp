on: [push]
name: CI
env:
  PROXY_PROJECT_NAME: kymp
jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1

      # @todo build proxy as well.
      - name: Build project
        run: make setup-ci
