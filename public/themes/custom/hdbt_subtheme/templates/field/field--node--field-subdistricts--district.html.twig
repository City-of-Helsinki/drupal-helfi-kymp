{%
  set title_classes = [
    'label',
    label_display == 'visually_hidden' ? 'visually-hidden',
  ]
%}

{% for item in items %}
  <span {{ attributes.addClass(title_classes) }} >{{ item.content }}</span>
{% endfor %}
